<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>真的有这么丝滑吗？MotionLayout+ViewDragHelper绝赞组合!</title>
      <link href="/2021/03/10/%E7%9C%9F%E7%9A%84%E6%9C%89%E8%BF%99%E4%B9%88%E4%B8%9D%E6%BB%91%E5%90%97-MotionLayout-ViewDragHelper/"/>
      <url>/2021/03/10/%E7%9C%9F%E7%9A%84%E6%9C%89%E8%BF%99%E4%B9%88%E4%B8%9D%E6%BB%91%E5%90%97-MotionLayout-ViewDragHelper/</url>
      
        <content type="html"><![CDATA[<h1 id="👏-MotionLayout-ViewDragHelper绝赞组合"><a href="#👏-MotionLayout-ViewDragHelper绝赞组合" class="headerlink" title="👏 MotionLayout+ViewDragHelper绝赞组合!"></a>👏 MotionLayout+ViewDragHelper绝赞组合!</h1><blockquote><p>内容可能存在错误或瑕疵，欢迎斧正…</p></blockquote><h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>目前各大视频、音乐播放器都有小窗（折叠）和全屏（展开）模式。但是对我而言，一直没找到任何一个产品能完美地通过用户交互在这两个模式之间优雅地过渡，唯一差强人意的是YouTube移动客户端。但是它对用户手指松开后的动画处理仍然不够自然。所以我思考着能不能自己实现一个。</p><h2 id="效果预览"><a href="#效果预览" class="headerlink" title="效果预览"></a>效果预览</h2><p><img src="/img/post_imgs/真的有这么丝滑吗-MotionLayout-ViewDragHelper/Video_20210310_043627_818.gif" alt="Video_20210310_043627_818"></p><hr><p>上面时我实现后的成品，展示了一个可根据用户拖动而在小窗和全屏之间过渡的音乐播放器。可以看到，无论是交互还是动画，都非常自然，也没有过度设计导致的低效。</p><h2 id="需求实现分析"><a href="#需求实现分析" class="headerlink" title="需求实现分析"></a>需求实现分析</h2><p>对于这样的需求，我首先想到的就是使用<code>ViewDragHelper</code>，以为它可以自然地处理手势拖拽。然后根据拖拽的比例设置控件的属性从而实现动画效果。但是这个界面上控件如此之多，各种控件位置、大小、透明度、颜色、可见性乃至圆角大小都在变化，且不考虑工作量问题，就算实现了可维护性也非常糟糕。如果中途需求更改，某处要添加、移除一个控件，或者更换某个控件的动画效果，都是会牵一发而动全身的。这就是我们为什么要引入<code>MotionLayout</code>，它可以声明式地实现两个布局之间的过渡动画。其实，<code>MotionLayout</code>本身也可以通过<code>&lt;OnSwipe/&gt;</code>标签实现手势拖拽，但是效果真的差强人意，因为它无法实现像<code>ViewDragHelper</code>一样，当手指松开后，自然地过渡到结束状态。只有<code>linear</code>、<code>accelerate</code>、<code>decelerate</code>三种插值选项。所以我们还是使用<code>ViewDragHelper</code>处理手势。</p><h2 id="MotionLayout简介"><a href="#MotionLayout简介" class="headerlink" title="MotionLayout简介"></a>MotionLayout简介</h2><blockquote><p><strong>MotionLayout</strong> is intended to move, resize, and animate UI elements with which <strong>users interact</strong>, such as buttons and title bars. Motion in your app should <strong>not</strong> be simply a gratuitous special effect in your application. It should be used to help users understand what your application is doing. </p></blockquote><p><code>MotionLayout</code>是谷歌 I/O 2018引入Material Design 2后不久推出的旨在实现丝滑交互动画的布局组件。官方设计指南特别强调，<code>MotionLayout</code>的存在不是为了简单的动画特效，而是为了处理与User Interact相关的UI过渡。它对动画的实现完全是声明式的，即你只需要声明动画的开始、结束状态、关键帧等，而不需要考虑动画的具体实现。它还内置各种效果超赞的<code>PahMotion</code>和插值器，Android Studio还提供可视化编辑器的支持<del>（我的体验非常糟糕）</del>。那么，这么强大的东西学习成本是不是很高？这里打个预防针，要实现我们的需求，并不需要对<code>MotionLayout</code>有深入的了解，掌握一些基础用法就足够了。</p><h2 id="ViewDragHelper简介"><a href="#ViewDragHelper简介" class="headerlink" title="ViewDragHelper简介"></a>ViewDragHelper简介</h2><p><code>ViewDragHelper</code>年代可就久远了，早在支持库年代就有了，大约可以追溯到2015年，MD一代推出之后。它的作用就如它的名字一样，帮助实现控件拖拽，网上的教程也是多如牛毛。我们会在下面的代码中介绍如何使用这玩意儿，使用起来也并不复杂。</p><h2 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h2><p>首先在你的项目中引入：</p><pre><code class="groovy">//内置ViewDragHelperimplementation &#39;androidx.appcompat:appcompat:1.2.0&#39;//须2.0+版本，内置MotionLayoutimplementation &#39;com.android.support.constraint:constraint-layout:2.0.4&#39;</code></pre><h2 id="配置MotionLayout布局"><a href="#配置MotionLayout布局" class="headerlink" title="配置MotionLayout布局"></a>配置MotionLayout布局</h2><p>首先，我们新建一个布局文件，命名为<code>fragment_player.xml</code>。为了解耦，我们使用<code>Fragment</code>，将播放器界面与主界面分开。打开此布局文件，可以看到默认根布局是<code>ConstranitLayout</code>，我们进入design模式，选中我们的根布局后单击鼠标右键。可以看到，有一个选项：<strong>Convert to MotionLayout</strong>：</p><p><img src="/img/post_imgs/真的有这么丝滑吗-Motionlayout-ViewDragHelper/image-20210310123457587.png" alt="image-20210310123457587"></p><p>点击后，可以发现我们布局的根节点变成了<code>MotionLayout</code>，并且自动添加了一个<code>app:layoutDescription</code>属性，该属性指向了一个<code>fragment_player_scene.xml</code>文件。没错，这个文件就是用于声明动画的，点进去：</p><pre><code class="xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot;?&gt;&lt;MotionScene    xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;    xmlns:motion=&quot;http://schemas.android.com/apk/res-auto&quot;&gt;    &lt;Transition        motion:constraintSetEnd=&quot;@+id/end&quot;        motion:constraintSetStart=&quot;@id/start&quot;        motion:duration=&quot;1000&quot;&gt;       &lt;KeyFrameSet&gt;       &lt;/KeyFrameSet&gt;    &lt;/Transition&gt;    &lt;ConstraintSet android:id=&quot;@+id/start&quot;&gt;    &lt;/ConstraintSet&gt;    &lt;ConstraintSet android:id=&quot;@+id/end&quot;&gt;    &lt;/ConstraintSet&gt;&lt;/MotionScene&gt;</code></pre><p>其中<code>Transition</code>节点定义了过渡动画的起始场景、结束场景、关键帧等；<code>ConstraintSet</code>节点则用于存放视图约束条件，这是什么意思呢？往后看。</p><p>接下来，我们试着往<code>MotionLayout</code>中增加一个<code>View</code>，用作播放器的背景。等等，你是不是有疑惑，为什么背景不直接设置<code>android:background</code>属性在<code>MotionLayout</code>上呢？是这样的，因为<code>MotionLayout</code>只支持其<strong>直接子View</strong>进行动画，而<code>MotionLayout</code>本身要求高度为<code>match_parent</code>，我们才能实现全屏的效果。所以既要支持背景动画，又要保证能全屏，我们就需要使用一个<code>View</code>来作为背景。</p><pre><code class="xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot;?&gt;&lt;!--  ~ Copyright (c) 2021 xjunz. All Rights Reserved.  --&gt;&lt;androidx.constraintlayout.motion.widget.MotionLayout xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;    xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot;    android:layout_width=&quot;match_parent&quot;    android:layout_height=&quot;match_parent&quot;    app:layoutDescription=&quot;@xml/sample_scene&quot;&gt;    &lt;View        android:id=&quot;@+id/player_background&quot;        android:background=&quot;#333&quot;        android:layout_width=&quot;wrap_content&quot;        android:layout_height=&quot;wrap_content&quot; /&gt;&lt;/androidx.constraintlayout.motion.widget.MotionLayout&gt;</code></pre><p>接下来，进入布局编辑器的design模式，可以看到<code>MotionLayout</code>的可视化编辑器，我们试着体验一下。</p><p><img src="/img/post_imgs/真的有这么丝滑吗-Motionlayout-ViewDragHelper/image-20210310132816444.png" alt="image-20210310132816444" style="zoom:67%;"></p><p>可以看到，右上有两个分别标着“start”和“end”的矩形页面，这就是我们<code>fragment_player_scene.xml</code>中定义的两个<code>Transition</code>节点的id。下方，可以编辑<code>ConstrantSet</code>。我们先选中”start“页面，再选中下方的<code>player_background</code>，然后点击红色方框中的按钮，选择<img src="/img/post_imgs/真的有这么丝滑吗-Motionlayout-ViewDragHelper/image-20210310132922715.png" alt="image-20210310132922715" style="zoom:67%;"></p><p>就可以在右侧工具栏像使用<code>ConstraintLayout</code>一样设置约束了。</p><p><img src="/img/post_imgs/真的有这么丝滑吗-Motionlayout-ViewDragHelper/image-20210310133032363.png" alt="image-20210310133032363" style="zoom:67%;"></p><p>我们把背景约束到布局底部，然后宽度设为<code>match_parent</code>，高度设为<code>?actionSize</code> (Action Bar的高度)。</p><p><img src="/img/post_imgs/真的有这么丝滑吗-Motionlayout-ViewDragHelper/image-20210310133532255.png" alt="image-20210310133532255" style="zoom:67%;"></p><p>设置完之后，再查看XML代码，发现诶？好奇怪，怎么XML没变化？是的，与<code>ConstraintLayout</code>不同的是，对View设置的所有约束都需要在<code>scene</code>文件中定义，而不是在<code>MotionLayout</code>中定义，编辑器已经自动帮我们重定向到<code>fragment_player_scene.xml</code>了：</p><pre><code class="xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot;?&gt;&lt;MotionScene xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;    xmlns:motion=&quot;http://schemas.android.com/apk/res-auto&quot;&gt;    &lt;Transition        motion:constraintSetEnd=&quot;@+id/end&quot;        motion:constraintSetStart=&quot;@id/start&quot;        motion:duration=&quot;1000&quot;&gt;        &lt;KeyFrameSet&gt;        &lt;/KeyFrameSet&gt;    &lt;/Transition&gt;    &lt;ConstraintSet android:id=&quot;@+id/start&quot;&gt;        &lt;!--约束节点--&gt;        &lt;Constraint            android:id=&quot;@+id/player_background&quot;            android:layout_width=&quot;match_parent&quot;            android:layout_height=&quot;?actionBarSize&quot;            motion:layout_constraintBottom_toBottomOf=&quot;parent&quot; /&gt;    &lt;/ConstraintSet&gt;    &lt;ConstraintSet android:id=&quot;@+id/end&quot;&gt;    &lt;/ConstraintSet&gt;&lt;/MotionScene&gt;</code></pre><p>这样<code>player_background</code>的初始约束就完成了。同理，当我们选中“end”页面，再为其添加结束约束：</p><pre><code class="xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot;?&gt;&lt;MotionScene xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;    xmlns:motion=&quot;http://schemas.android.com/apk/res-auto&quot;&gt;    &lt;Transition        motion:constraintSetEnd=&quot;@+id/end&quot;        motion:constraintSetStart=&quot;@id/start&quot;        motion:duration=&quot;1000&quot;&gt;        &lt;KeyFrameSet&gt;        &lt;/KeyFrameSet&gt;    &lt;/Transition&gt;    &lt;ConstraintSet android:id=&quot;@+id/start&quot;&gt;        &lt;Constraint            android:id=&quot;@+id/player_background&quot;            android:layout_width=&quot;match_parent&quot;            android:layout_height=&quot;?actionBarSize&quot;            motion:layout_constraintBottom_toBottomOf=&quot;parent&quot; /&gt;    &lt;/ConstraintSet&gt;    &lt;ConstraintSet android:id=&quot;@+id/end&quot;&gt;        &lt;Constraint            android:id=&quot;@+id/player_background&quot;            android:layout_width=&quot;match_parent&quot;            android:layout_height=&quot;wrap_content&quot;            motion:layout_constraintTop_toTopOf=&quot;parent&quot;            motion:layout_constraintBottom_toBottomOf=&quot;parent&quot; /&gt;    &lt;/ConstraintSet&gt;&lt;/MotionScene&gt;</code></pre><p>此时回到可视化编辑器：</p><p><img src="/img/post_imgs/真的有这么丝滑吗-Motionlayout-ViewDragHelper/image-20210310135030327.png" alt="image-20210310135030327" style="zoom:67%;"></p><p>先点击上方红色框部分，再点击播放按钮，就可以预览我们的动画了!</p><p><img src="/img/post_imgs/真的有这么丝滑吗-Motionlayout-ViewDragHelper/GIF 2021-3-10 13-55-30.gif" alt="GIF 2021-3-10 13-55-30" style="zoom: 67%;"></p><p>除了约束可以被动画以外，我们还可以设置各种<code>transform</code>属性:</p><p><img src="/img/post_imgs/真的有这么丝滑吗-Motionlayout-ViewDragHelper/image-20210310135806189.png" alt="image-20210310135806189"></p><p>甚至是可见性<code>visibility</code>，<code>MotionLayou</code>t也会自动使用透明度动画来过渡。除此之外，还支持<strong>View Transition</strong>属性，和自定义<strong>Attributes</strong>：</p><p><img src="/img/post_imgs/真的有这么丝滑吗-Motionlayout-ViewDragHelper/image-20210310140143967.png" alt="image-20210310140143967"></p><p>这些用法可以参考官方文档或网上的博文，本人不再赘述，因为这不是重点。</p><p>现在如果你在<code>Transition</code>节点下使用<code>OnSwipe</code>标签：</p><pre><code class="xml">&lt;MotionScene xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;    xmlns:motion=&quot;http://schemas.android.com/apk/res-auto&quot;&gt;    &lt;Transition        motion:constraintSetEnd=&quot;@+id/end&quot;        motion:constraintSetStart=&quot;@id/start&quot;        motion:duration=&quot;1000&quot;&gt;        &lt;OnSwipe            motion:dragDirection=&quot;dragUp&quot;            motion:touchAnchorId=&quot;@id/player_background&quot;            motion:touchAnchorSide=&quot;top&quot; /&gt;    &lt;/Transition&gt;    ...&lt;/MotionScene&gt;</code></pre><p>就可以在“start”和“end”约束之间拖动<code>player_background</code>。但是如我前面所言，<code>MotionLayout</code>自带的拖动释放效果非常拉跨，我们需要使用<code>ViewDagHelper</code>来配合。</p><h2 id="使用ViewDragHelper"><a href="#使用ViewDragHelper" class="headerlink" title="使用ViewDragHelper"></a>使用ViewDragHelper</h2><p>新建一个类<code>PlayerLayout</code>，继承自<code>MotionLayout</code>，并在其中初始化<code>ViewDragHelper</code>:</p><pre><code class="java">public class PlayerLayout extends MotionLayout {    private ViewDragHelper mHelper;    private ViewDragHelper.Callback mCallback;    /**     * background view     */    private View mBackground;    /**     * background view在折叠时候的Top     */    private int mTopWhenCollapsed;    /**     * background view在折叠时候的高度     */    private int mHeightWhenCollapsed;    public PlayerLayout(@NonNull Context context, @Nullable AttributeSet attrs) {        super(context, attrs);        init();    }    public PlayerLayout(@NonNull Context context) {        super(context);        init();    }    public PlayerLayout(@NonNull Context context, @Nullable AttributeSet attrs, int defStyleAttr) {        super(context, attrs, defStyleAttr);        init();    }    private void init() {        mHelper = ViewDragHelper.create(this, mCallback);        OneShotPreDrawListener.add(this, () -&gt; {            //在这里获取top和height            mTopWhenCollapsed = mBackground.getTop();            mHeightWhenCollapsed = mBackground.getHeight();        });    }     @Override    protected void onSizeChanged(int w, int h, int oldw, int oldh) {        super.onSizeChanged(w, h, oldw, oldh);        //当布局大小改变时(如软键盘弹起)，更新top        mTopWhenCollapsed = h - mHeightWhenCollapsed;    }    @Override    protected void onFinishInflate() {        super.onFinishInflate();        mBackground = findViewById(R.id.player_background);    }    @Override    public boolean onInterceptTouchEvent(@NotNull MotionEvent event) {        //让helper判断是否应该切断事件        return mHelper.shouldInterceptTouchEvent(event);    }    @SuppressLint(&quot;ClickableViewAccessibility&quot;)    @Override    public boolean onTouchEvent(@NotNull MotionEvent event) {        //这里判断一下helper是否处于拖拽状态，拖拽状态就返回true消耗掉        //不是的话将事件传递下去交给其他View处理，否则MotionLayout会        //block掉整个屏幕的触摸事件        mHelper.processTouchEvent(event);        boolean block = mHelper.getCapturedView() != null;        //拖动的时候禁止打断（ACTION_CANCEL）        requestDisallowInterceptTouchEvent(block);        return block;    }}</code></pre><p>上面很多代码是<code>ViewDragHelper</code>固定的用法，有不懂的可以看注释。<code>mTopWhenCollapsed</code>后面会用到。我们还有一个<code>mCallback</code>变量未初始化，因为这个比较重要，单独拿出来：</p><pre><code class="java">mCallback = new ViewDragHelper.Callback() {    @Override    public boolean tryCaptureView(@NonNull View child, int pointerId) {        //在这里定义允许哪个子视图拖动        //我们允许MotionLayout中的所有子视图拖动        return true;    }    @Override    public void onViewCaptured(@NonNull View capturedChild, int activePointerId) {        super.onViewCaptured(capturedChild, activePointerId);        try {            //因为MotionLayout只支持其直接子视图的动画，而我们需要允许所有子视图拖动            //这样的话，就需要处理非常多的View的拖动,因此重定向我们的capturedChild            //到background view, 这样就只要处理一个view就够了            Field capturedViewField = mHelper.getClass().getDeclaredField(&quot;mCapturedView&quot;);            capturedViewField.setAccessible(true);            capturedViewField.set(mHelper, mBackground);        } catch (NoSuchFieldException | IllegalAccessException e) {            //ho hum        }    }    @Override    public void onViewPositionChanged(@NonNull View changedView, int left, int top, int dx, int dy) {        super.onViewPositionChanged(changedView, left, top, dx, dy);        if (dy == 0) {            return;        }        //当background view拖动位置改变时，我们获取其拖动的百分比        //mTopWhenCollapsed就是能拖动的总距离        float fraction = 1 - mBackground.getTop() / (float) mTopWhenCollapsed;//from 0 to 1        //在这里根据拖动的百分比设置动画进度        setProgress(fraction);    }    @Override    public void onViewReleased(@NonNull View releasedChild, float xvel, float yvel) {        //当手指松开取消拖动时        super.onViewReleased(releasedChild, xvel, yvel);        //根据background view的速度、位置分别处理打开还是关闭player        if (yvel &gt; 0) {            collapsePlayer();        } else if (yvel &lt; 0) {            expandPlayer();        } else {            if (releasedChild.getTop() / (float) mTopWhenCollapsed &gt; .5f) {                collapsePlayer();            } else {                expandPlayer();            }        }    }    @Override    public int getViewVerticalDragRange(@NonNull View child) {        //返回background view所能垂直拖动的距离        return mTopWhenCollapsed;    }    @Override    public int clampViewPositionVertical(@NonNull View child, int top, int dy) {        //约束background view在拖动中垂直方向上所允许top范围        //返回值约束在0和mTopWhenCollapsed之间        return Math.min(Math.max(0, top), mTopWhenCollapsed);    }    @Override    public int clampViewPositionHorizontal(@NonNull View child, int left, int dx) {        //水平方向自适应        return child.getLeft();    }};</code></pre><p>最后，是松手后实现自然折叠、展开的方法：</p><pre><code class="java">public void expandPlayer() {    if (mHelper.smoothSlideViewTo(mBackground, 0, 0)) {        ViewCompat.postInvalidateOnAnimation(this);    }}public void collapsePlayer() {    if (mHelper.smoothSlideViewTo(mBackground, 0, mTopWhenCollapsed)) {        ViewCompat.postInvalidateOnAnimation(this);    }}@Overridepublic void computeScroll() {    if (mHelper.continueSettling(true)) {        ViewCompat.postInvalidateOnAnimation(this);    }}</code></pre><p>这样，就完成了最基本的框架就完成了，核心思想就是在<code>ViewDragHelper.Callback</code><strong>中监听滑动的比例，然后为<code>MotionLayout</code>设置动画的比例</strong>，当然还有很多小细节的处理。预览图那样的效果不过是在此基础上增加了更多的<code>view</code>和动画。</p><h2 id="其他冷知识"><a href="#其他冷知识" class="headerlink" title="其他冷知识"></a>其他冷知识</h2><h3 id="如何渐变圆角？"><a href="#如何渐变圆角？" class="headerlink" title="如何渐变圆角？"></a>如何渐变圆角？</h3><p>可以看到我们的预览图中，播放器折叠时左上角有圆角，但是随着滑动，圆角逐渐消失最终不见。这是如何实现的呢？</p><h4 id="MaterialShapeDrawable"><a href="#MaterialShapeDrawable" class="headerlink" title="MaterialShapeDrawable"></a>MaterialShapeDrawable</h4><p>使用前先导入google的design库：</p><pre><code class="groovy">implementation &#39;com.google.android.material:material:1.2.1&#39;</code></pre><p>构造实例并使用：</p><pre><code class="java">//构造左上角圆角的ShapeAppearanceModelmDefShapeAppearance = ShapeAppearanceModel.builder().setTopLeftCorner(CornerFamily.ROUNDED, defCornerSize).build();//根据ShapeAppearanceModel初始化MaterialShapeDrawablemBackgroundDrawable = new MaterialShapeDrawable(mDefShapeAppearance);//将此Drawable设为view的背景view.setBackground(mBackgroundDrawable);</code></pre><p>提供改变圆角的方法：</p><pre><code class="java">public void setTopLeftCornerSize(float cornerSize) {  mBackgroundDrawable.setShapeAppearanceModel(mDefShapeAppearance.toBuilder().setTopLeftCorner(CornerFamily.ROUNDED, cornerSize).build());}</code></pre><h4 id="Custom-Attributes"><a href="#Custom-Attributes" class="headerlink" title="Custom Attributes"></a>Custom Attributes</h4><p>如果想要<code>MotionLayout</code>自动在动画过程中改变圆角，而不用手动设置，可以使用上面提及的<strong>自定义属性</strong>。新建一个自定义View继承自你想设置圆角的那个View的类，然后把上面设置圆角的方法内置到该类中。</p><p>在编辑器中选中该view，在其约束编辑窗口中找到此按钮<img src="/img/post_imgs/真的有这么丝滑吗-Motionlayout-ViewDragHelper/image-20210310151249634.png" alt="image-20210310151249634" style="zoom:67%;">然后会弹出此窗口：</p><p><img src="/img/post_imgs/真的有这么丝滑吗-Motionlayout-ViewDragHelper/image-20210310151412639.png" alt="image-20210310151412639" style="zoom:67%;"></p><p>在“start”和“end”两个<code>ConstranitSet</code>中分别配置<code>Value</code>就行了。</p><h3 id="如何渐变状态栏颜色"><a href="#如何渐变状态栏颜色" class="headerlink" title="如何渐变状态栏颜色?"></a>如何渐变状态栏颜色?</h3><pre><code class="java">//前提是windowTranslucentStatus=false且windowDrawsSystemBarBackgrounds=trueObjectAnimator.ofArgb(activity.getWindow(), &quot;statusBarColor&quot;, customColor);</code></pre>]]></content>
      
      
      <categories>
          
          <category> 安卓开发 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>自定义Shared Element Transition注意事项</title>
      <link href="/2021/03/08/%E8%87%AA%E5%AE%9A%E4%B9%89Shared%20Element%20Transition%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9/"/>
      <url>/2021/03/08/%E8%87%AA%E5%AE%9A%E4%B9%89Shared%20Element%20Transition%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9/</url>
      
        <content type="html"><![CDATA[<h1 id="📢-自定义Shared-Element-Transition注意事项"><a href="#📢-自定义Shared-Element-Transition注意事项" class="headerlink" title="📢 自定义Shared Element Transition注意事项"></a>📢 自定义Shared Element Transition注意事项</h1><blockquote><p><code>Activity Shared Element Transition</code> (<strong>ASET</strong>) 和<code>Fragment Shared Element Transition</code> (<strong>FSET</strong>) 是有本质不同的。因此，自定义Transition想要同时兼容这两种模式，其实并不简单。</p></blockquote><blockquote><p>内容可能有错误或瑕疵，待完善…</p></blockquote><h2 id="从A到B的ASET"><a href="#从A到B的ASET" class="headerlink" title="从A到B的ASET"></a>从A到B的ASET</h2><p>系统会将B中View的一些属性设为A中View的值，然后根据初始值和终值的不同构造执行<code>Transition</code>。如果你自定义的<code>Transition</code>旨在<code>Activity</code>间使用，那么在<code>captureStartValues</code>中获取初始值时需要注意，此时获取到的View是<strong>Activity B</strong>中共享元素，而<strong>不是</strong>Activity A中的那个共享元素。能获取到的信息只有位置、大小、可见性等信息，其他信息无法获得，只能通过xml或代码指定。但是在<code>SharedElmentCallback#onSharedElementStart()</code>，可以获取到<strong>Activity A</strong>的元素的<code>Snapshot Bitmap</code> ，此View是源共享元素的快照，可以将其添加到<code>Overlay</code>中伪造源View。</p><h3 id="为什么要这样设计？"><a href="#为什么要这样设计？" class="headerlink" title="为什么要这样设计？"></a>为什么要这样设计？</h3><p>原因在于Activity之间的Transition数据是通过<code>Intent</code>传递的，所以不可能将整个View传递到下一个Activity，只能传递某些可以序列化的属性，如大小、位置、快照等。虽然有局限性，但是也有其优越性：这意味着，只要对接完好，即使是两个不同应用的Activity也可以进行共享元素Transition。</p><h3 id="Shared-Element-Return-Transition"><a href="#Shared-Element-Return-Transition" class="headerlink" title="Shared Element Return Transition"></a>Shared Element Return Transition</h3><blockquote><p>Shared Element Return Transition (SERT) 在ASET中也是非常特殊的存在，需要注意以下问题。</p></blockquote><h4 id="初始状态问题"><a href="#初始状态问题" class="headerlink" title="初始状态问题"></a>初始状态问题</h4><p>对于SERT，需要注意的是，在SERT之前系统会把视图设置为SERT的<code>end</code>状态（也是Shared Elment Enter Transition的<code>start</code>状态）保证<code>captureEndValues</code>能正常工作。因此，如果要执行动画，必须将视图手动重置为SERT的<code>start</code>状态。比如，你需要执行一个位移动画，那么，请先通过<code>view.layout</code>将View移动到初始位置，动画才能如期（as expected）执行。</p><h4 id="最后一帧问题"><a href="#最后一帧问题" class="headerlink" title="最后一帧问题"></a>最后一帧问题</h4><p>需要知道的是，Transition都是在 <strong>Activity B</strong>中执行的。在SERT执行完成之后，B会被<code>finish</code>。考虑到这一点，需要特别注意动画的<code>onAnimationEnd</code>回调是否有改变共享元素外观的代码（比如移除Overlay中的Drawable/View），因为<code>onAnimationEnd</code>是在动画最后一帧执行的，在<code>onAnimationEnd</code>和<code>finish</code>之间就会有一帧的空隙，如果不处理好，会出现视图一闪而过的突变，体验非常糟糕。</p><h2 id="从A到B的FSET"><a href="#从A到B的FSET" class="headerlink" title="从A到B的FSET"></a>从A到B的FSET</h2><p>但是对于FSET，在<code>captureValues</code>中获取到的View就分别是A中的共享元素和B中共享元素。通过<code>captureValues</code>获到任何需要的初始值和终值。当然，对于从View-&gt;View的Transition更是如此，因此，构造动画基本上不需要外部指定数据。</p><p>不同于ASET: Enter Transition在B中执行，Return Transition在A中执行，FSET的Transition都是在目标<code>Fragment</code>中执行，对视图执行的动画最后都会停留在布局中，而不会像ASET在执行完后因为<code>finish</code>而销毁。所以需要考虑的问题就没有那么多。</p>]]></content>
      
      
      <categories>
          
          <category> 安卓开发 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Databinding使用笔记</title>
      <link href="/2021/03/08/Databinding%E4%BD%BF%E7%94%A8%E7%AC%94%E8%AE%B0/"/>
      <url>/2021/03/08/Databinding%E4%BD%BF%E7%94%A8%E7%AC%94%E8%AE%B0/</url>
      
        <content type="html"><![CDATA[<h1 id="Databinding使用中的一些细节"><a href="#Databinding使用中的一些细节" class="headerlink" title="Databinding使用中的一些细节"></a>Databinding使用中的一些细节</h1><blockquote><p>可能存在错误或瑕疵，待完善…</p></blockquote><ul><li><p>实体类的字段要想实现可观察，其字段需要使用<code>ObservableXxx</code>。如果不想使用<code>ObservableXxx</code>，该实体类可继承自<code>BaseObservable</code>，然后在对应的字段的<code>getter</code>上添加<code>@Bindable</code>注解，在对应的<code>setter</code>里调用<code>notifyPropertyChange</code>，这样该字段仍然是可观察的，如果你既不想使用<code>Observable</code>，而且该类已经继承自其他类，无法再继承<code>BaseObservable</code>。那么可以参照<code>BaseObservable</code>的写法引入<code>PropertyChangeRegistry</code>以及相关方法。这样做看似不优雅，但是其实是官方操作，因为Google官方的例子中就是这样做的，参见<a href="https://github.com/android/databinding-samples/blob/master/TwoWaySample/app/src/main/java/com/example/android/databinding/twowaysample/util/ObservableViewModel.kt" target="_blank" rel="noopener">databinding-samples</a> 。</p></li><li><p><code>@BindingAdapter</code>注解，该注解可以实现自定义XML属性。比如，你想实现一个<code>View</code>根据XML中定义的<code>width</code>设置宽度，那么，我们可以定义一个<code>android:width</code>属性: </p><pre><code class="xml">&lt;layout xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;&gt;    &lt;data&gt;        &lt;variable            name=&quot;width&quot;            type=&quot;int&quot; /&gt;    &lt;/data&gt;        &lt;View              ...            android:width=&quot;@{width}&quot;/&gt;&lt;/layout&gt;</code></pre><p>但是此时，<code>Databinding</code>框架是无法识别<code>android:width</code>的，因为View没有<code>setWidth</code>方法，此时我们要定义一个方法来告诉框架如何使用此属性：</p><pre><code class="java">  @BindingAdapter(&quot;android:width&quot;)   public static void setWidth(@NotNull View view, int width) {          ViewGroup.LayoutParams lp = view.getLayoutParams();          lp.width = width;          view.setLayoutParams(lp);   }</code></pre><p>注意，该方法可以放在任何一个可视性为<code>public</code>的类中，并且其修饰符须为<code>public static</code>(除非自定义Component)，方法名是任意的。建议新建一个类专门存放这些<code>Binding Adapters</code>，官方就是这么做的： 如所有<code>TextView</code>有关的自定义绑定适配器都存放在<code>TextViewBindingAdapter</code>中。该方法第一个参数是XML中的目标<code>View</code>对象，第二个参数是XML中传入的值。这样定义后，IDE就能识别该自定义属性，Databinding框架就会自动调用此方法绑定数据。</p></li><li><p><code>@InverseBindingAdapter</code>注解，该注解用于双向绑定。如果说<code>@BindingAdapter</code>定义了如何为视图设置特定值，那么<code>@InverseBindingAdapter</code>就定义了如何从视图中获取特定值。如果某个自定义属性要实现双向绑定，必须同时实现<code>@BindingAdapter</code>注解和<code>@InverseBindingAdapter</code>注解。还是上面这个例子，如果我们需要实现视图的宽度改变时，自动改变XML中<code>width</code>变量的值，我们需要对XML做如下改动：</p><blockquote><p> android:width=”@<strong>=</strong>{width}”</p></blockquote><p>然后实现反向绑定对应的方法:</p><pre><code class="java">@InverseBindingAdapter(attribute = &quot;android:width&quot;,event = &quot;android:widthAttrChanged&quot;)public static int getWidth(@NotNull View view) {    return view.getWidth();}</code></pre><p>这样，框架就知道如何从视图中获取<code>android:width</code>属性的值，以便传递给<code>width</code>变量。但是问题是，框架知道如何获取值，但是它并不知道该值何时改变啊，所以目前双向绑定还没有生效。不知你是否注意到上面代码中的<code>event = &quot;android:widthAttrChanged&quot;</code>。是的，顾名思义，当 <code>android:widthAttrChanged</code> 这个事件发生时，框架会读取该值。这个事件名默认为<code>andoroid:xxxAttrChanged</code>, xxx为我们自定义的属性名，所以这段<code>event = &quot;android:widthAttrChanged&quot;</code>其实可以省略。接下来的问题是，<code>widthAttrChanged</code>事件是啥，什么时候发生？为此，我们还需要配置一个额外的Binding Adapter：</p><pre><code class="java">@BindingAdapter(&quot;android:widthAttrChanged&quot;)public static void setWidthWatcher(View view, final InverseBindingListener listener) {    View.OnLayoutChangeListener newValue = (v, left, top, right, bottom, oldLeft, oldTop, oldRight, oldBottom) -&gt; {        boolean isChanged = left != oldLeft || right != oldRight || top != oldTop || bottom != oldBottom;        if (isChanged)            listener.onChange();    };    View.OnLayoutChangeListener oldValue = ListenerUtil.trackListener(view, newValue, R.id.onWidthChanged);    if (oldValue != null) {        view.removeOnLayoutChangeListener(oldValue);    }    view.addOnLayoutChangeListener(newValue);}</code></pre><p>是不是看懵了？解释一下。可以看到，这个Binding Adapter定义了一个<code>android:widthAttrChanged</code>属性，没错，就是反向绑定触发事件名（<strong>注：该自定义属性不可用于XML</strong>），该方法其实就是定义了该事件何时触发。首先，该方法有两个参数，第一个是目标视图，第二个是反向绑定监听器。反向绑定监听器就是一个通知视图属性改变，然后反向改变数据的回调。查看方法体，可以看到，我们为该View设置了一个<code>OnLayoutChangeListener</code>, 目的是监听该View的宽度改变，当宽度改变时，调用<code>InverseBindingListener#onChange()</code>，这样就相当于发出了<code>widthAttrChanged</code>事件。不过，方法体中好像还有一些意义不明的代码，这些代码其实是防止重复监听的模板代码。其中<code>ListenerUtil.trackListener</code>可以追踪指定指定view的指定id的listener，如果该view已经有指定id的listener，就会返回该listener，否则返回null。最后一个问题，既然这个自定义属性不能在XML中使用，那这个方法什么时候会调用呢？当框架检测到我们的<code>android:width</code>的双向绑定，就会查找该属性反向绑定事件所在的<code>@BindingAdapter</code>注解并调用此方法，<code>InverseBindingListener</code>由系统传入。</p></li><li><p>如果自定义属性不存在，Databinding框架会自动查找该视图的<code>getter</code>和<code>setter</code>方法，而不用手动定义Adapters。关于这个特性，可以有骚操作。当我们自定义View时，可能会需要自定义属性，那么，通常情况下，需要在<code>&lt;declare-styleable/&gt;</code>标签下自定义<code>attr</code>，然后在代码中解析。利用Databinding，我们只需要定义好对应的<code>getter</code>和<code>setter</code>（须符合Java bean 规范）方法，就可以直接在xml中直接使用自定义属性了，省去大量工作。当然这样做布局编辑器是无法预览这些自定义属性的。</p></li><li><p>如果自定义属性的<code>setter</code>和<code>getter</code>方法已经存在，只是名称不符合<code>setter</code>和<code>getter</code>规范。此时，可以使用<code>@BindingMethods</code>和<code>@InverseBindingMethods</code>注解进行声明，而不需要手动定义Adapters。参见<code>AdapterViewBingAdapter</code>等官方实现类。</p></li><li><p>Databinding表达式支持空值判断三元运算语法糖：<code>@{a??b}=@{a==null?b:a}</code></p></li><li><p>默认数据绑定是在数据更新后一帧执行，如果遇到需要改变数据后立即更新View的需求，可以调用<code>executePendingBinding</code>立即刷新数据绑定。通常，遇到数据和View的某些属性无法对应的情况，可能就是掉进了这个坑。比如：某个View的enabled状态是由XML中的变量<code>enabled</code>指定的，如果在代码中设置了<code>enabled=true</code>，然后立刻（在一个事件循环内）获取<code>isEnabled</code>，就会发现变量<code>enabled</code>的值和View的状态并不一致。</p></li><li><p><code>@BindingAdapter</code>的<code>requiredAll</code>属性的作用在于，当<code>requireAll</code>为<code>true</code>时（默认），只有当View同时定义了<code>value</code>中的所有属性，才会调用此方法（否则会报错）。当<code>requiredAll</code>为<code>false</code>时，只要定义了<code>value</code>中的任意一个属性，就会调用此方法。</p></li><li><p>XML中属性文本定义时的先后位置确定这些属性在数据绑定时的先后顺序，如果某个属性依赖前一个属性，那么确保它们的位置顺序是正确的。如果无法确保（比如IDE格式化XML后会自动更换顺序），或者为了提高可维护性，那么请使用上面提到的<code>requireAll</code>属性，建立一个<code>@BindingAdapter</code>，在<code>value</code>中同时包含这两个属性，并使用<code>requireAll=true</code>，然后在方法体中自己定义两个属性的设置顺序。</p></li><li><p>Databinding表达式中语句最后的空括号可省略，比如<code>onClick=&quot;@{()-&gt;activity.onClick(edit.getText())}&quot;</code>可省略为<code>onClick=&quot;@{()-&gt;activity.onClick(edit.getText)}&quot;</code>，但是如果语句为<code>onClick=&quot;@{()-&gt;activity.onClick(edit.getText().toString())}&quot;</code>,那么，<br>只能省略为<code>onClick=&quot;@{()-&gt;activity.onClick(edit.getText().toString)}&quot;</code>而不能省略为<code>onClick=&quot;@{()-&gt;activity.onClick(edit.getText.toString)}&quot;</code></p></li><li><p>如果在表达式中引用<strong>其他视图</strong>的属性并使用了无get语句(如<code>.getText()</code>变成<code>.text</code>)，那么系统会默认你在使用这个属性的反向绑定，比如<code>text=&quot;@{edit.text.toString()}&quot;</code>，那么当<code>edit.text</code>改变时，此控件的<code>text</code>也会改变（系统已经提前实现了text属性的反向绑定方法），如果你使用了一个未实现反向绑定的属性，如<code>padding=&quot;@{textView.height}&quot;</code>，那么，构建应用时系统就会报错，此时你要自己使用<code>@InverseBindingAdapter</code>和<code>@BindingAdapter</code>来获取并通知<code>height</code>的变化，此方法才可用。如果只是想引用某个属性，而不是与某个属性绑定，那么请使用未省略get的语句，如<code>padding=&quot;@{textView.getHeight()}&quot;</code>。这时候，只有执行数据绑定时，两个数据才会同步，而不是当一个改变时，另一个也改变。其实系统这样的设计是正确的，通常情况下，我们引用另一个视图的属性，就是为了保持与之同步。</p></li><li><p>在xml中调用方法传参为<code>null</code>时，如果不将<code>null</code>显式强转为参数所要求的类型，Databinding框架会将<code>null</code>强转为<code>Object</code>类型，若该方法所需参数不是<code>Object</code>，那么就会无法通过编译。</p></li><li><p><code>ViewDataBinding.inflate(inflater)</code>直接可以获得实例，我居然用<code>ViewDataBindingUtils.inflate()</code>用了这么久。</p></li></ul>]]></content>
      
      
      <categories>
          
          <category> 安卓开发 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>快速笔记 (长期更新...)</title>
      <link href="/2021/01/01/%E5%BF%AB%E9%80%9F%E7%AC%94%E8%AE%B0-%E9%95%BF%E6%9C%9F%E6%9B%B4%E6%96%B0/"/>
      <url>/2021/01/01/%E5%BF%AB%E9%80%9F%E7%AC%94%E8%AE%B0-%E9%95%BF%E6%9C%9F%E6%9B%B4%E6%96%B0/</url>
      
        <content type="html"><![CDATA[<h1 id="📖-快速笔记"><a href="#📖-快速笔记" class="headerlink" title="📖 快速笔记"></a>📖 快速笔记</h1><blockquote><p>这里用来记录一些单独拿出来篇幅太短但是又不能一句话说完的笔记…</p></blockquote><blockquote><p>内容可能有错误或瑕疵，待完善…</p></blockquote><h2 id="Weak-Reference"><a href="#Weak-Reference" class="headerlink" title="Weak Reference"></a>Weak Reference</h2><p>当一个对象只有<code>Weak Reference</code>引用的时候，允许被GC回收。比如有这样的需求：从网络上下载了一个大型图片，暂时存在内存中。这个图片需要从<strong>Activity A</strong>传递到<strong>Activity B</strong>，因为超过1MB，无法通过<code>Intent</code>传递。此时聪明的我想到，反正都是一个进程里的Activity，何必一定要用<code>Intent</code>传递数据。为什么不用一个静态类寄存这个图片然后向外提供访问方法呢？于是我新建了一个<code>BitmapRegistry</code>采用单例模式，Activity A获取单例在其中寄存这个图片，然后Activity B获取单例读取图片。看似很完美，但是却存在隐藏的内存泄露。因为<code>BitmapRegistry</code>持有了静态单例，该单例的生命周期大于Activity的生命周期，当Activity退出时，<code>BitmapRegistry</code>单例仍然持有对图片的强引用，那么GC就不会回收这张图片所占用的内存。应对这种情况，<code>WeakReference</code>就有用武之地了。如果使用<code>WeakReference</code>包装该图片，那么当Activity被销毁时（假设只有这个Activity使用了这个图片），就只剩下了<code>BitmapRegistry</code>对该图片的弱引用，GC仍然会回收这张图片。同理，<code>WeakHashMap</code>就是基于这样的理念，当它的value只有key对他的弱引用时，其对应的value就会允许被GC回收。</p><h2 id="Try-With-Resources"><a href="#Try-With-Resources" class="headerlink" title="Try-With-Resources"></a>Try-With-Resources</h2><p>某天写代码时看到Android Studio提示可以将<code>try{}finially{}</code>语句自动优化为 <code>try-with-resources</code>语句，我点了一下，发现代码变成了这样：</p><pre><code class="java">try (Cursor cursor = getDatabase().rawQuery(&quot;...&quot; , null) {            if (cursor.moveToNext()) {                return cursor.getString(0);            }            return null;        }</code></pre><p>网上搜了以下发现，try括号内的变量要实现<code>AutoClosable</code>接口，就能使用此语句了。</p><h2 id="String的深度克隆"><a href="#String的深度克隆" class="headerlink" title="String的深度克隆"></a>String的深度克隆</h2><p>今天突然有一个需求，修改一个text后，点击“恢复”按钮，让此text恢复成原来的字符串。我在考虑要不要克隆一个原字符串当作备份用于恢复，于是乎便上网搜索String的克隆，这一搜才知道，String类型和基本类型一样，只要赋值给另一个变量，这个变量就是原变量的深度克隆了，举个例子:</p><blockquote><p>String a=”a”; String b=a; a=”b”;</p></blockquote><p>此时，打印出b，其内容仍然是<strong>“a”</strong>。因为<code>String</code>一旦创建后，其内容是不可改变，所有<code>String</code>都会储存进常量池，你对一个<code>String</code>进行赋值，只会让它指向常量池中的一个字符串，而不会让其内容改变。因此，当我们对b赋值为a时，b实际上指向了常量池中<strong>“a”</strong>的地址，当我们对a赋值为<strong>“b”</strong>时，a实际上指向了常量池中<strong>“b”</strong>的地址，因此，改变a并不会改变b的值，所以b就是实际意义上的a的深度克隆。</p><h2 id="Service笔记"><a href="#Service笔记" class="headerlink" title="Service笔记"></a>Service笔记</h2><p>关于启动Service的方式有两种，一种是<code>startService</code>，一种是<code>bindService</code>，前者适合那些不用与UI交互的Service，后者适合需要与UI交互的Service。因为<code>bindService</code>可以通过<code>Binder</code>很方便地在Activity与Service之间通讯。<code>startService</code>启动的Service会一直在后台运行，即使启动它的Activity已经销毁；但是<code>bindService</code>所启动的Service会在其Client Activity销毁时一起销毁（假设没有其他client与他绑定）。Service的<code>onCreate</code>方法只会在其启动时执行一次。假如一个Service已经在后台运行，那么再调用<code>bindService</code>或者<code>startService</code>都不会在调用<code>onCreate</code>方法，因此<code>onCreate</code>方法里只适合做一些初始化工作。会调用<code>onStartCommand</code>方法，因此，可以在<code>onStartCommand</code>方法里做一些更新/恢复操作。通过<code>bindService</code>再<code>startService</code>可以实现即能与UI互动，又不会因为Activity被销毁而销毁的Service。</p>]]></content>
      
      
      <categories>
          
          <category> 安卓开发 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 长期更新 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>安卓开发中的奇技淫巧（长期更新...）</title>
      <link href="/2021/01/01/%E5%AE%89%E5%8D%93%E5%BC%80%E5%8F%91%E4%B8%AD%E7%9A%84%E5%A5%87%E6%8A%80%E6%B7%AB%E5%B7%A7/"/>
      <url>/2021/01/01/%E5%AE%89%E5%8D%93%E5%BC%80%E5%8F%91%E4%B8%AD%E7%9A%84%E5%A5%87%E6%8A%80%E6%B7%AB%E5%B7%A7/</url>
      
        <content type="html"><![CDATA[<h1 id="🎃-开发中记录下的一些小Tricks"><a href="#🎃-开发中记录下的一些小Tricks" class="headerlink" title="🎃 开发中记录下的一些小Tricks"></a>🎃 开发中记录下的一些小Tricks</h1><blockquote><p>一些内容可能包含错误或瑕疵，待完善… </p></blockquote><ul><li><p>activity如果设为<code>windowIsTranslucent=true</code></p><p>那么，它的默认进入退出动画不再是系统默认动画，而是非常原始的滑入滑出动画。因为系统会自动设置过渡动画为<code>@android:style/Animation.Translucent</code></p></li><li><p><code>RecyclerView</code>的Item并不是离开屏幕就会被重新回收利用，这意味着一个Item滑出屏幕再划回来并不一定会调用<code>onBindViewHolder</code>更新Item，可以通过<code>setItemViewCacheSize</code>方法设置缓存的Item数，默认的缓存数是2。</p></li><li><p><code>ColorUtils.compositeColors</code>可以根据前景色和背景色合成颜色，还有<code>ColorUtils</code>里的其他方法。</p></li><li><p>父控件设置<code>android:descendantFocusability=&quot;beforeDescendants&quot;</code>属性可用切断子控件的焦点，触摸事件会直接穿透到父控件，变成父控件的触摸事件。特别适用于操控<code>Checkable</code>视图的父布局。</p></li><li><p>保留两位小数<strong>%.2f</strong>；保留两位小数的百分比<strong>%.2f%%</strong>，”%%”是“%”的转义，注意数值要乘以100才行。 或者使用<code>NumberFormat.getPercentInstance()</code></p></li><li><p><code>Objects.equals(Object a,Object b)</code>，比较两个对象是否相同并且<strong>允许为<code>null</code></strong>。</p></li><li><p><code>ConstraintLayout</code>中的<code>layout_goneMargin</code>属性，可以让被约束的View的margin仍然生效，即使目标View的<code>visibility</code>已为<code>gone</code>。</p></li><li><p>动态设置<code>textStyle</code>的方法：使用<code>setTypeface(Typeface typeface,int style)</code>，注意第一个参数不要使用<code>getTypeface()</code>，这样设置会导致第二个参数style如果为<code>Typeface.NORMAL</code>无效，第一个参数用null即可。</p></li><li><p>判断某个对象是否为数组<code>obj.getClass().isArray()</code>，注意基本类型的数组是不继承自<code>Object[]</code>的，直接继承自<code>Object</code>。因此，基本类型数组不能强转为 <code>Object[]</code>。</p></li><li><p>才发现有<code>MessageFormat</code>这个类，太好用了。</p></li><li><p>在style中加入</p><pre><code class="xml">   &lt;item name=&quot;android:windowSoftInputMode&quot;&gt;adjustResize&lt;/item&gt;</code></pre><p>   可以让布局自动根据软键盘resize。</p></li><li><p><code>view.supressLayout</code>可以阻止布局进行layout，该方法虽然在<strong>API 29</strong>才被引入，但是其实在<strong>API 18</strong>就已经有了，只是未设为公开， 添加这两个注解消除编译器警告。</p><pre><code class="java">   @RequiresApi(18)   @SuppressLint(&quot;NewApi&quot;)</code></pre></li><li><p>androidx中的工具类: <code>OneShotOnPreDrawListener</code>，快速一次性监听<code>onPreDraw</code>事件。</p></li><li><p><code>setMovementMethod(LinkMovementMethod.getInstance())</code>，可以让<code>TextView</code>支持点击<code>&lt;/a&gt;</code>标签，也可支持自定义协议，比如在清单标签中为<code>Activity</code>设置<code>IntentFilter</code>标签：</p><pre><code class="xml">&lt;action android:name=&quot;android.intent.action.VIEW&quot; /&gt;&lt;data android:scheme=&quot;[自定义协议]&quot; /&gt;&lt;category android:name=&quot;android.intent.category.DEFAULT&quot; /&gt;&lt;category android:name=&quot;android.intent.category.BROWSABLE&quot; /&gt;</code></pre><p>点击文字就可以跳转到<code>Activity</code>，根据<code>Intent</code>执行自定义逻辑。</p></li><li><p><code>layout_constarintedWidth</code>，即使<code>layou_width</code>设为<code>wrap_content</code>也适用约束，类似于<code>maxWidth</code>效果，非常好用的属性。类似地，还有<code>layout_constarintedHieght</code>。</p></li><li><p><code>shouldInterceptTouchEvent</code>如果返回<code>true</code>，就会截获事件，所有<code>MotionEvent</code>都会走<code>onTouchEvent</code>，并且接下来的<code>MotionEvent</code>不会再走<code>shouldInterceptTouchEvent</code>，直到<code>ACTION_UP</code>；如果<code>return false</code>, 所有 <code>MotionEvent</code>都不会走<code>onTouchEvent</code>，则会交由parent寻找合适的目标处理，接下来的所有事件依然会走<code>shouldInterceptTouchEvent</code>直到<code>ACTION_UP</code>; 如果已交由正确的目标处理<code>MotionEvent</code>后<code>return true</code>，该目标就会收到一个<code>ACTION_CANCEL</code>事件。 </p></li><li><p>TextView走马灯效果很多时候不生效，但是以下配置实测生效：</p><pre><code class="xml">&lt;!--Databinding适用中--&gt;&lt;!--suppress AndroidUnknownAttribute --&gt;&lt;TextView        android:layout_width=&quot;wrap_content&quot;        android:layout_height=&quot;wrap_content&quot;        android:ellipsize=&quot;marquee&quot;        android:focusable=&quot;true&quot;        android:focusableInTouchMode=&quot;true&quot;         android:selected=&quot;@{true}&quot;        android:singleLine=&quot;true&quot;&gt;        &lt;requestFocus /&gt;&lt;/TextView&gt;</code></pre></li><li><p>View的Fading Edge特性。Fading Edge可以让可滚动的View在其边缘显示渐变遮罩，提示用户仍有内容可以滚动，设置如下：</p><pre><code class="xml">   &lt;!--Databinding适用中--&gt;   &lt;!--suppress AndroidUnknownAttribute --&gt;   &lt;RecyclerView       android:fadingEdgeLength=&quot;@dimen/spacing_large&quot;       android:requiresFadingEdge=&quot;vertical&quot;       android:willNotDraw=&quot;@{false}&quot;       ...     /&gt;</code></pre></li></ul>]]></content>
      
      
      <categories>
          
          <category> 安卓开发 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 长期更新 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>关于我搭博客这件事</title>
      <link href="/2019/04/25/%E5%85%B3%E4%BA%8E%E6%88%91%E6%90%AD%E5%8D%9A%E5%AE%A2%E8%BF%99%E4%BB%B6%E4%BA%8B/"/>
      <url>/2019/04/25/%E5%85%B3%E4%BA%8E%E6%88%91%E6%90%AD%E5%8D%9A%E5%AE%A2%E8%BF%99%E4%BB%B6%E4%BA%8B/</url>
      
        <content type="html"><![CDATA[<h1 id="关于起因"><a href="#关于起因" class="headerlink" title="关于起因"></a>关于起因</h1><p>其实早在2016年，我就了解到Hexo框架下的精致博客，那是Material主题作者在简书发表的推广简介。那篇文章在我的收藏夹躺到现在。眼看该项目最近一条commit都是九个月前了，我才搭好，感觉像是看了一份糊在墙上的报纸。因为当时我觉得搭博客是一件非常麻烦的事，所以还没尝试就放弃了。最近，看到一位好友的博客，我又心血来潮，才下定决心。这一路下来，发现其实并不难。所以说，有时阻挡我们的不是荆棘，而是退却的心。</p><h1 id="关于过程"><a href="#关于过程" class="headerlink" title="关于过程"></a>关于过程</h1><p>我完全不懂Web知识，搭博客几乎就是照着别人的教程一步一步下来的。从挑域名到成功deploy大约花了四五小时，虽然是成功了，但是一路都是云里雾里的，完全不懂为什么，只知道Ctrl C+Ctrl V…回头再看，才有了初步的了解。接下来就是一堆配置工作，我这个从未接触过yaml的小白以至于不知道冒号后面要加空格而被坑了好一会儿，生而为人，我很抱歉。</p><h1 id="一些细节"><a href="#一些细节" class="headerlink" title="一些细节"></a>一些细节</h1><p>关于如何搭建hexo以及如何托管到github（抑或是自己的服务器）我就不赘述了，网上教程及官方文档已经说得很明白了。</p><h2 id="关于Material-主题的使用"><a href="#关于Material-主题的使用" class="headerlink" title="关于Material 主题的使用"></a>关于Material 主题的使用</h2><p>很简单，直接把项目<code>clone</code>到<code>hexo</code>的<code>theme</code>目录下</p><pre><code class="powershell">cd [你的主题目录]git clone https://github.com/viosey/hexo-theme-material.git</code></pre><p>然后修改hexo的<code>_config.yml</code>，找到theme字段，修改为clone下来的文件夹名，名字可以修改。没修改过的话，应该就是<code>hexo-theme-material</code>，以下都以 hexo-theme-material为例</p><pre><code class="yaml">theme: hexo-theme-material</code></pre><p>还有一个坑， <strong>需要手动将 hexo-theme-material 文件夹中的 <code>_config.template.yml</code> 复制一份并重命名为 <code>_config.yml</code></strong>。</p><p>最后，如果你的博客是托管在github上，并且使用了自定义域名，请在<strong>主题</strong>的<code>source</code>目录下新建一个文件，命名为<code>CNAME</code>, 然后在里面写入你的域名。这样github就会自动识别<code>custom domain</code>，而不用你每次都去手动配置。现在</p><pre><code class="powershell">hexo cleanhexo ghexo d</code></pre><p>就能看到主题部署成功啦！接下来就是按自己喜好配置主题咯！</p><h1 id="一些小问题"><a href="#一些小问题" class="headerlink" title="一些小问题"></a>一些小问题</h1><h3 id="如何建立一个自我介绍的页面？"><a href="#如何建立一个自我介绍的页面？" class="headerlink" title="如何建立一个自我介绍的页面？"></a>如何建立一个自我介绍的页面？</h3><p>先创建一个新页面，如下，该命令会在hexo的<code>source</code>目录下创建一个文件夹，这个文件夹和你的<code>_posts</code>是平行且隔离的。</p><pre><code class="powershell">hexo new page &quot;intro[文件夹名可自定义，以下就以intro为例]&quot;</code></pre><p>成功后，<code>intro</code>文件夹下会生成一个<code>index.md</code>,不同于post页面，自定义的页面只能识别index.md并显示，你新建其他md，是不会被显示的。现在你就可以在index.md里写自己的介绍啦！当然，写完以后，如何在侧边栏添加一个入口呢？首先，打开<strong>主题</strong>的<code>_config.yml</code>,找到<code>sidebar</code>节点，然后找到sidebar节点下的<code>pages</code>节点,最后在pages节点下添加</p><pre><code class="yaml">  关于: #这里的文字会显示在侧边栏，可以自定义      link: &quot;/intro&quot; # 这里是生成的page的目录，即我前面指令里的intro，不能乱改      icon: person #可以自定义图标，图标来自于material.io/icons,无需下载图标，直接填图标名即可      divider: false #是否显示下方分割线</code></pre><p>完成后 <code>hexo  s</code>，就可以预览效果啦！</p><h3 id="如何在博客中使用图片？"><a href="#如何在博客中使用图片？" class="headerlink" title="如何在博客中使用图片？"></a>如何在博客中使用图片？</h3><p>看到这个大佬可能笑了，怎么插入图片还要你教？等等，我的意思是如何在在线网页上使用你写博客时用的本地的图片。大佬可能会说用图床啊！当然这是一种方法，但是还有更方便的方法。以Typora为例（看下图），插入图片时可选择<strong>设置图像根目录</strong>，第一步，我们把它设置为，<strong><code>...\hexo-theme-material\source</code></strong>。第二步，我们可以看到有个<strong>复制图片到文件夹</strong>的选项，可以点击配置，当然如果不想每次写新博客都手动配置呢，我们可以点击<strong>全局图像设置</strong><img src="/img/post_imgs/typro2.PNG" alt></p><p>打开全局图像配置后，选择“复制到指定目录”<img src="/img/post_imgs/typora3.PNG" alt></p><p>最关键的部分来啦！这时，在你的<strong><code>...\hexo-theme-material\source\img</code></strong>文件夹下新建一个post_imgs文件夹（文件夹名字随意，主要用于存放博客的图片，以下以<code>post_imgs</code>为例），然后将<strong>复制到指定目录</strong>的目标目录选为<strong><code>...\hexo-theme-material\source\img\post_imgs\${filename}</code></strong>，即你新建的目录的路径，如上图。现在，你在博客中插入图片，可以看到，我们的图像路径是<code>/img</code>开头的<img src="/img/post_imgs/TIM图片20190427110356.png" alt></p><p>而这正是网页寻址的相对目录，这意味着，我们的图片就能被网页正常引用，只要配置好了，网页博客插入图片就和在本地写博客一样简单，最后运行 <code>hexo d</code> ,看看效果。<del>唯一的缺点就是每次写新博客就得花几秒钟配置一下图片根目录，Typora也没有全局的选项。</del>打开博客根目录，找到<code>scaffolds</code>（脚手架）文件夹，这里面定义了每次<code>hexo new</code>新建文章的模板，打开<code>post.md</code>，加入一行：</p><blockquote><p>typora-root-url: ....\themes\hexo-theme-material\source\</p></blockquote><p>这样Typora就能自动识别根目录了。</p><h3 id="如何建立时光轴、标签云、图库等？"><a href="#如何建立时光轴、标签云、图库等？" class="headerlink" title="如何建立时光轴、标签云、图库等？"></a>如何建立时光轴、标签云、图库等？</h3><p>其实建立这些的原理和我上面说的建立“自我介绍”页面时一样的，本质也是建立一个新page,只不过这个page里的index.md的layout类型是特定的罢了。具体的方法参考上文以及<a href="https://neko-dev.github.io/material-theme-docs/#/pages" target="_blank" rel="noopener">官方文档</a>。</p><h3 id="如何在博客主页添加网易云音乐歌单-歌曲外链播放器？"><a href="#如何在博客主页添加网易云音乐歌单-歌曲外链播放器？" class="headerlink" title="如何在博客主页添加网易云音乐歌单/歌曲外链播放器？"></a>如何在博客主页添加网易云音乐歌单/歌曲外链播放器？</h3><p>这是什么呢？看图，或者到我的博客主页底部就能看到这个<img src="/img/post_imgs/outchain.png" alt></p><p>那这是怎么是怎么实现的呢？其实主要得益于网易云音乐提供的外链播放器功能，首先登录网页版网易云音乐，登录以后，点击进入一个歌单，如下图<img src="/img/post_imgs/outchain1.png" alt></p><p>点击图片上指示的<strong>生成外链播放器</strong>，复制他给的代码就行了，一般像这样：</p><pre><code class="html">&lt;iframe frameborder=&quot;no&quot; border=&quot;0&quot; marginwidth=&quot;0&quot; marginheight=&quot;0&quot; width=330 height=450 src=&quot;//music.163.com/outchain/player?type=0&amp;id=135453255&amp;auto=0&amp;height=430&quot;&gt;&lt;/iframe&gt;</code></pre><p>此时，如果你的歌单里包含没有版权的歌曲，网页会提示是无法生成外链播放器。这时看我图示标注的歌单id，把生成的外链中的id参数替换掉就行，即使这样，没有版权的歌曲还是无法播放的，但是聊胜于无嘛。当然，自己建立一个博客专用歌单也是阔以的。</p><p>最关键的步骤来啦，以我的Material主题为例，打开<strong><code>...\themes\hexo-theme-material\layout\index.ejs</code></strong>这个文件，看图<img src="/img/post_imgs/outchain2.png" alt></p><p>选择合适的位置粘贴你的代码，我选择了主页最底部，当然如果你想置顶，把它放到<code>Normal Post</code>上面就行，不过可能没有那么协调。</p>]]></content>
      
      
      <categories>
          
          <category> 杂谈 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2019/04/23/hello-world/"/>
      <url>/2019/04/23/hello-world/</url>
      
        <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><pre><code class="bash">$ hexo new &quot;My New Post&quot;</code></pre><p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><pre><code class="bash">$ hexo server</code></pre><p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><pre><code class="bash">$ hexo generate</code></pre><p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><pre><code class="bash">$ hexo deploy</code></pre><p>More info: <a href="https://hexo.io/docs/deployment.html" target="_blank" rel="noopener">Deployment</a></p>]]></content>
      
      
      
    </entry>
    
    
  
  
    
    
    <entry>
      <title>Gallery</title>
      <link href="/gallery/index.html"/>
      <url>/gallery/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>Timeline</title>
      <link href="/timeline/index.html"/>
      <url>/timeline/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>About Me</title>
      <link href="/About-me/index.html"/>
      <url>/About-me/index.html</url>
      
        <content type="html"><![CDATA[<h1 id="关于"><a href="#关于" class="headerlink" title="关于"></a>关于</h1><p>目前的博客框架是<a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>,  主题是<a href="https://github.com/viosey/hexo-theme-material" target="_blank" rel="noopener">Material</a> ，Markdown编辑器是好用又美观的<a href="https://www.typora.io/" target="_blank" rel="noopener">Typora</a> .</p><h2 id="我的称呼"><a href="#我的称呼" class="headerlink" title="我的称呼"></a>我的称呼</h2><p><strong>XJUNZ</strong>  ( 读作: <em>/eksdʒʌnz/</em> )</p><h2 id="我来自"><a href="#我来自" class="headerlink" title="我来自"></a>我来自</h2><p>浙江</p><h2 id="年龄"><a href="#年龄" class="headerlink" title="年龄"></a>年龄</h2><p>95后  狮子座</p><h2 id="爱好特长"><a href="#爱好特长" class="headerlink" title="爱好特长"></a>爱好特长</h2><p>没想好…</p><h2 id="长话短说"><a href="#长话短说" class="headerlink" title="长话短说"></a>长话短说</h2><p>懒| 拖延症|好想回到童年|Material Design 拥趸| Praise Google</p><p><em>待补充…</em></p><h2 id="联系到我"><a href="#联系到我" class="headerlink" title="联系到我"></a>联系到我</h2><p><strong>QQ</strong> (主要): 1409122313</p><p><strong>Telegram </strong>(随缘上线): xjunz</p>]]></content>
      
    </entry>
    
    
  
</search>
